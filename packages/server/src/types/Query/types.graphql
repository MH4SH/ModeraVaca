type Query {
	users(limit: Int, cursor: String): UserCursor!
	user(id: ID!): User

	datas: DataCursor!
	data(id: ID!): Data

	purchases: PurchaseCursor!
	purchase(id: ID!): Purchase

	sales: SaleCursor!
	sale(id: ID!): Sale

	births: DeadCursor!
	birth(id: ID!): Dead

	deads: DeadCursor!
	dead(id: ID!): Dead
}