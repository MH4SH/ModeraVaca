type Query {
  users: UserCursor!
  user(id: ID!): User!

  datas: DataCursor!
  data(id: ID!): Data!

  purchases: PurchaseCursor!
  purchase(id: ID!): Purchase!

  sales: SaleCursor!
  sale(id: ID!): Sale!

  borns: BornCursor!
  born(id: ID!): Born!
}