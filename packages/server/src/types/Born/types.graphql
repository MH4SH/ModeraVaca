type Born {
    id: ID!
    idBreeds: ID!
    gender: GenderType!
    amount: Int!
    dateBorn: Int!
    note: String
}

input BornInput {
    idBreeds: ID!
    gender: GenderType!
    amount: Int!
    dateBorn: Int!
    note: String
}

type BornCursor {
    edges: [BornEdge!]!
    pageInfo: PageInfo!
}

type BornEdge {
    node: Born!
    cursor: String
}