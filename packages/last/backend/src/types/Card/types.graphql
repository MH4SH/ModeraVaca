type Card {
    id: ID!
    name: String!
}
input CardInput {
    name: String
}

type CardCursor {
    edges: [CardEdge]
    pageInfo: PageInfo!
}

type CardEdge {
    node: Card
    cursor: String
}